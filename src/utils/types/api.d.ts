type Usertype = {
  type: string;
  iconString: string;
  description?: string;
};

type Interest = { type: string; iconString: string };

type MathLevelType = {
  name: string;
  description: string;
  preview: any;
};
type LearningPath = {
  id: string;
  lable?: string;
  headerText: string;
  headerDescription: string;
  imageSrc: string;
};
